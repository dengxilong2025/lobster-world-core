# P1 里程碑复盘（2026-04-16）

里程碑范围：P1（观战读模型 + 回放 + 稳定性）

## 0) 基本信息
- 里程碑：P1
- 时间范围：2026-04-16
- 参与者：熙龙 / 小梦

## 1) 验收结果（当前代码仓库）
通过项（本机/CI `go test ./...` 全绿）：
- 观战首页：`GET /api/v0/spectator/home`（Projection 输出，热点榜按热度排序）
- 实体页：`GET /api/v0/spectator/entity`（关系网摘要 + 近期事件 + why_strong + next_risk）
- 回放：`GET /api/v0/replay/highlight`（trace 驱动的纪录片式 beats；无 trace 有降级）
- SSE 续传：`GET /api/v0/events/stream?since_ts=...` 支持补拉，并验证补拉后仍能收到 live

未覆盖/待增强：
- 读模型持久化（目前内存投影，支持从 EventStore 冷启动重建，但未落盘读库）
- 关系强度（当前 strength 固定 1；后续引入权重/衰减）
- 回放可视化（当前是脚本回放 beats，尚未生成视频/图片）

## 2) 做得好的
1) 读写解耦清晰：观战与回放全走 Projection/事件事实源，后续可扩展不侵入写侧。
2) 测试牵引：新增功能先写集成测试，保证 API 行为不漂移。
3) 鲁棒性：trace 优先、无 trace 自动降级；SSE 有补拉路径，避免慢消费者丢体验。

## 3) 问题与风险
1) 当前 /intents 仍为占位执行器，真实“文明秀蝴蝶效应”需要 tick 仿真与策略落地。
2) 热点榜与 why_strong/next_risk 目前是启发式 v0，需要逐步数据化与可调参（运营可控）。
3) 事件体积增长后，EventStore/Query 性能与分页策略需要增强（tick 分片、索引、快照）。

## 4) 行动项（P2/P3 候选）
- [ ] 引入 tick 仿真内核（写侧）替换占位执行器（owner：小梦）
- [ ] 关系强度与衰减模型（owner：小梦）
- [ ] 观战读模型落盘（投影到读库/缓存），并加负载测试（owner：小梦）

