# P0 里程碑复盘（2026-04-16）

里程碑范围：P0（Task0–Task7）

> 复盘模板来源：`/workspace/docs/18_milestone_retrospective_template.md`（云端文档库）

## 0) 基本信息
- 里程碑：P0（身份/事件事实源/事件流/意图最小闭环/领养解绑/CI门禁）
- 时间范围：2026-04-16
- 参与者：熙龙 / 小梦

## 1) 验收结果
### 1.1 验收脚本/测试
通过的验收项（证据：你本机 `go test ./...` 全绿）：
- `/healthz` 集成测试通过
- Auth：challenge/prove + `/api/v0/me` 集成测试通过
- Events：`/api/v0/events` 拉取 + `/api/v0/events/stream` SSE 推送集成测试通过
- Intents：`/api/v0/intents` 最小闭环通过（事件流产生 intent_accepted/action_started/action_completed）
- Adoption：confirm/revoke + 冷却期集成测试通过
- CI：GitHub Actions workflow 已添加（push/PR 自动跑 `go test ./...`）

未通过项：
- 无（P0 目标已达成）

### 1.2 指标（P0阶段先做“技术指标”）
- 本机测试耗时：集成测试约 2.5s（后续会随功能增长，需要分层与并行优化）

## 2) 做得好的（保留）
1) 事件规范（Event Spec）先立约束，再围绕它做 EventStore/SSE/投影，减少返工。
2) 严格 TDD：先红后绿，每次交付前本机与CI都能复测。
3) “2–4个任务一包”的同步节奏，减少操作成本但不牺牲质量门禁。

## 3) 问题清单（事实）
1) 云端环境 Go 工具链非预装，导致无法直接持续跑测试；已通过“下载go到临时目录”解决，但需要流程化。
2) 当前 v0 事件推送 hub 采用非阻塞丢弃策略（慢消费者会丢事件），后续必须补“补拉”（since_ts/tick）机制与 backpressure 设计。
3) `/intents` 目前是占位执行器（sleep+发事件），需要在 P1/P2 替换为真正 tick 仿真队列。

## 4) 根因分析（选最大风险：事件丢失）
- 问题：SSE 客户端可能因为网络/慢消费丢事件
- Why1：hub Publish 非阻塞，缓冲满会丢
- Why2：v0 没有“断线续传/补拉”协议约束
- 结论：必须在 P1 引入“事件拉取 + since_ts/tick 续订”的标准流程，并在测试中覆盖断线续传。

## 5) 决策与行动项（落地）
- [ ] P1-M1：实现观战读模型投影（`/spectator/home`）并定义缓存策略（owner：小梦）
- [ ] P1-M2：实现回放生成器最小版本（`/replay/highlight`）并从事件 narrative/trace 生成 beats（owner：小梦）
- [ ] P1-RISK：补齐 SSE 断线续传测试（owner：小梦）

## 6) 测试与稳定性改进
- 已建立：集成测试覆盖 Auth/Events/Intents/Adoption
- 下一步：增加断线续传/补拉测试；引入简单的负载基准（例如 100 SSE 连接的 smoke test）

## 7) 下一里程碑展望（P1）
1) 北极星目标：观战端“3分钟看懂”的数据契约落地并可被 API 返回
2) 最大风险：观战读模型与回放内容“像日志不像纪录片”——需要强制 narrative/trace 规则
3) 需要对标：Nakama/Colyseus 的观战与消息流实践（仅借鉴，不照搬）

