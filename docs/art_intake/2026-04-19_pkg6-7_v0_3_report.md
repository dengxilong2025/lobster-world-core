# 资源包盘点报告（6-7/7）— GAME_ASSETS_v0.3.zip & GAME_ASSETS_v0.3_fixed.zip

> 这两个包 **文件列表完全一致**（都是 48 个文件，其中 PNG 47 + MD 1），差异几乎都体现在“透明通道修复”。  
> 我会以 `v0.3_fixed` 为准入库，并明确标出仍需小文明天返工的“含字图标”。

---

## 1) 两包一致性与差异

### 1.1 目录结构
两包目录结构相同：
- `tiles/`（10）
- `ui/frames`（9）
- `ui/badges`（4）
- `ui/icons`（24）
- `scenes/`（1）
- `README.md`（1）

### 1.2 文件差异统计
- only_in_v0.3：0
- only_in_v0.3_fixed：0
- 同路径但内容不同：**40**

差异文件主要集中在：
- 全部 UI（frames/badges/icons）
- 部分 tiles（grass/sand/stone）

### 1.3 透明通道修复效果（关键）
这两包最大的区别是 **alpha 全 255（等同不透明）** 的数量：
- `v0.3`：alpha_all255 **43**（几乎都没有真正透明）
- `v0.3_fixed`：alpha_all255 **3**（透明已基本修复）

结论：**v0.3_fixed 明显更可用**。

---

## 2) 质量结论（能否直接用）

### ✅ 可以入库/可直接采用（以 v0.3_fixed 为准）
- `ui/frames`、`ui/badges`：UI 九宫格与标签底图
- 大多数 `ui/icons`：语义图标（intent/evolve/food/wood/stone/shock...）
- `tiles/tile_grass_basic_01_w256_h256.png`：目前看是“插画式地表块”，可用作场景块占位
- `scenes/spectate_bg_w1024_h1024.png`：观战背景（RGB，无透明属正常）

### ❌ 必须返工（仍含字）
以下图标仍含字/字母（硬红线），建议小文明天重做：
- `ui/icons/ui_icon_event_w64_h64.png`（有 “hi.”）
- `ui/icons/ui_icon_event_w128_h128.png`（有 “hi.”）
- `ui/icons/ui_icon_betrayal_w64_h64.png`（有字母 “A”）
- `ui/icons/ui_icon_betrayal_w128_h128.png`（有字母 “A”）

### ⚠️ 需要注意的异常（v0.3_fixed 中仍存在）
`tiles/tile_sand_basic_01_w256_h256.png` 在 v0.3_fixed 里出现大面积透明破洞（疑似误抠/通道错误）。  
建议：暂不放入 production（仅 intake 存档），或让小文明天重做更稳。

---

## 3) 入库计划（本次执行）
我会把 `v0.3_fixed`：
- 放入 `assets/intake/xiaowen/pkg6-7_v0_3_fixed/`
- 将“合格可用”的 UI/tiles 更新到 `assets/production/`
- 但会 **排除**：
  - 含字的 event/betrayal icons
  - `tile_sand_basic_01`（透明异常）

