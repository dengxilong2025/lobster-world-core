# 资源包盘点报告（2/5）— GAME_ASSETS_P0_DELIVERY.zip

> 结论先说：这一包比第 1 包成熟很多，**技术规格基本到位**（RGBA、尺寸、命名都对）。  
> 主要问题集中在两类：  
> 1) 个别 UI icon **含文字**（必须返工）  
> 2) 部分资源存在“抠图过度导致内部高光/白色细节变透明洞”的迹象（我已批量修复一版）

---

## 1) 目录结构与数量统计

本包 PNG 总数：**141**

目录分布：
- `tiles_individual/`：104（256×256 为主，含建筑/道具/装饰/路径/水岸等）
- `ui/`：37（frames/badges/icons）

尺寸分布（Top）：
- 256×256：105
- 128×128：12
- 64×64：12
- 32×32：4
- 256×64：4
- 其它（32×128、128×32）：少量

格式/透明：
- `RGBA`：141/141（全部带 alpha）
- 文件名 `_w.._h..` 与实际像素尺寸：**一致（0 个 mismatch）**

---

## 2) 可用性分级（按“能否先上 v0.2 站起来”）

### ✅ 可直接采纳（P0 可用）

**UI frames（九宫格切片）+ badges 底图**  
尺寸与命名已符合规范，透明也存在，整体风格也比较接近羊皮纸/部落感。

**tiles_individual 中的大部分 props / buildings / paths**  
可以作为地图上的“装饰/建筑/道具贴片”先上，后续再做统一风格精修也不影响流程跑通。

### ⚠️ 可采纳，但我建议“先用我修复版”

你提到的“抠图把内部白色/高光也扣掉”的问题，确实在这一包里出现过一些迹象。  
我做了一个**保守修复**：

- 只修“被扣成透明洞的内部亮细节”（**封闭洞**且 RGB 很亮的部分）
- 不动外轮廓（避免把背景粘回去）
- 顺手做了透明边缘的去黑边（defringe，防止贴到深色背景时出黑晕）

修复统计：
- 处理图片：141
- **检测并修复到的图片数：67**
- **补回的像素总数：1903**（仅内部亮洞）

修复包见：`asset_processing/pkg2_fixed.zip`

### ❌ 必须返工（明天让小文重做）

**UI icons 中含文字/字母（违反“图片无字”铁律）**：
- `ui/icons/ui_icon_event_w64_h64.png`（底部有 “hi.”）
- `ui/icons/ui_icon_event_w128_h128.png`（底部有 “hi.”）
- `ui/icons/ui_icon_betrayal_w64_h64.png`（有字母 “A”）
- `ui/icons/ui_icon_betrayal_w128_h128.png`（有字母 “A”）

> 这些我不建议“硬修”——自动去字很容易把主体也抹花。  
> 明天让小文重做这 2 个 icon（event / betrayal），保持同样风格但去掉任何字母即可。

---

## 3) 这包对“瓦片地图”的覆盖情况

这一包的 `tiles_individual/` 更像“单体贴片/摆件/建筑块”，而不是 **Type A 地表无缝纹理瓦片**。

目前看到的“地表类 tile”非常少（主要是 `tile_water_shore_v*.png`），**缺少 P0 最关键的：grass/dirt/sand/rock 的 256×256 无缝纹理瓦片**。

因此：  
- 本包可以先用于“地图装饰/建筑/道具贴片”  
- **Type A 基础地表瓦片仍需小文明天按手册 9.6 补做**

---

## 4) 给小文明天的返工指令（最短版）

1) 重做 icon：`event`、`betrayal`  
   - 保持同样风格与尺寸（64/128），**去掉任何文字/字母**
2) 补做 Type A 地表瓦片（P0 最小集合）：grass/dirt/sand/rock（256×256、无缝平铺）  
3) 尽量不要做“先抠图”——给我白底原图也行，我这边统一做透明处理，避免误抠高光

